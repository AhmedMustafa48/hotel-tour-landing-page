import React from 'react'

const Products = ({value, name}) => {
  return (
    <div>
        <h1>My name is {name} and the value after click button is {value}</h1>
        </div>
  )
}

export default Products